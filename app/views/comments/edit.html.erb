<% content_for :title, "Editing comment" %>

<h1>Editing comment</h1>

<%= render "form", comment: @comment, post: @post %>

<br>

<div>
  <%= link_to "Show this comment", post_path(@post, anchor: "comment-#{@comment.id}") %> |
  <%= link_to "Back to post", post_path(@post) %>
</div>
