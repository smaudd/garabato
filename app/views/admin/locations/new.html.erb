<% content_for :page_title, "Nuevo local" %>

<%= link_to "Volver a #{@company.name}", admin_company_path(@company), class: "back-link" %>

<div class="form-card">
  <%= render "form", company: @company, location: @location %>
</div>
