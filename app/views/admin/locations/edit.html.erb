<% content_for :page_title, t('.page_title') %>

<%= link_to t('.back'), admin_company_location_path(@company, @location), class: "back-link" %>

<div class="form-card">
  <%= render "form", company: @company, location: @location %>
</div>
