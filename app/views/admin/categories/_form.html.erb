<%= form_with model: [:admin, category] do |form| %>
  <% if category.errors.any? %>
    <div class="form-errors">
      <strong><%= t('admin.shared.errors', count: category.errors.count) %></strong>
      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, placeholder: t('.placeholder_name') %>
  </div>

  <div class="form-actions">
    <%= form.submit %>
    <%= link_to t('.cancel'), admin_categories_path, class: "btn btn-secondary" %>
  </div>
<% end %>
