<%= form_with model: [:admin, category] do |form| %>
  <% if category.errors.any? %>
    <div class="form-errors">
      <strong><%= pluralize(category.errors.count, "error") %> impidieron guardar:</strong>
      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, "Nombre" %>
    <%= form.text_field :name, placeholder: "Nombre de la categoría" %>
  </div>

  <div class="form-actions">
    <%= form.submit %>
    <%= link_to "Cancelar", admin_categories_path, class: "btn btn-secondary" %>
  </div>
<% end %>
