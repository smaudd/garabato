<% content_for :page_title, t('.page_title') %>

<%= link_to t('.back'), admin_category_path(@category), class: "back-link" %>

<div class="form-card">
  <%= render "form", category: @category %>
</div>
