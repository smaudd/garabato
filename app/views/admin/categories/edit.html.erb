<% content_for :page_title, "Editar categoría" %>

<%= link_to "Volver", admin_category_path(@category), class: "back-link" %>

<div class="form-card">
  <%= render "form", category: @category %>
</div>
